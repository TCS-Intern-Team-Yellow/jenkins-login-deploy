pipeline {
	agent any
	stages {
		stage('Sample') {
			steps {
				git 'https://github.com/TCS-Intern-Team-Yellow/login.git'
				sh 'ls'
				sh 'docker build -t loginservice .'
				sh 'docker images ls'
			}
		}
	}
}
