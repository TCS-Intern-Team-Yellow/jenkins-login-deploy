pipeline {
	agent any
	stages {
		stage('Sample') {
			steps {
				git 'https://github.com/TCS-Intern-Team-Yellow/login.git'
				sh 'ls'
				sh 'sudo docker build -t loginservice .'
				sh 'sudo docker images ls'
			}
		}
	}
}
